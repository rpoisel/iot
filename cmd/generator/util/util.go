package util

// func NodeREDExportDump(n *NodeREDExport) {
// 	for idx, element := range n.Elements {
// 		if element.Type == "mqtt-broker" {
// 			fmt.Printf("MQTT Broker | Host: %s, Port: %s, UseTLS: %v\n",
// 				element.Broker, element.Port, element.UseTLS)
// 			continue
// 		}
// 		fmt.Printf("Node | ID: %s, Name: %s, Type: %s, Wires: %v",
// 			element.ID, element.Name, element.Type, element.Wires)
// 		if element.Type == "mqtt in" {
// 			fmt.Printf(", Topic: %s", n.Fields[idx]["topic"])
// 		}
// 		fmt.Println()
// 	}
// }
